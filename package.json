{
  "name": "chudpresence-solo-extension",
  "version": "1.5.0",
  "private": true,
  "type": "module",
  "description": "Extension-only foundation for ChudPresenceSolo.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=18" }
}
