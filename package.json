{
  "name": "chudpresence-solo-extension",
  "version": "1.9.0",
  "private": true,
  "type": "module",
  "description": "Local Chromium MV3 Activity API V1 lab for ChudPresence Solo.",
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.mjs"
  },
  "engines": { "node": ">=20" }
}
